
function disa() {
    const button = document.querySelector('#btn-disabled');

    const disableButton = () => {
        button.disabled = true;

    };
    button.addEventListener('click', disableButton);
}

disa();

function disa2() {
    const button = document.querySelector('#btn-disabled1');

    const disableButton = () => {
        button.disabled = true;

    };
    button.addEventListener('click', disableButton);
}

disa2();


function disa3() {
    const button = document.querySelector('#btn-disabled2');

    const disableButton = () => {
        button.disabled = true;

    };
    button.addEventListener('click', disableButton);
}

disa3();


function disa4() {
    const button = document.querySelector('#btn-disabled3');

    const disableButton = () => {
        button.disabled = true;

    };
    button.addEventListener('click', disableButton);
}

disa4();


function disa5() {
    const button = document.querySelector('#btn-disabled4');

    const disableButton = () => {
        button.disabled = true;

    };
    button.addEventListener('click', disableButton);
}

disa5();



function disa6() {
    const button = document.querySelector('#btn-disabled5');

    const disableButton = () => {
        button.disabled = true;

    };
    button.addEventListener('click', disableButton);
}

disa6();



function disa7() {
    const button = document.querySelector('#btn-disabled6');

    const disableButton = () => {
        button.disabled = true;

    };
    button.addEventListener('click', disableButton);
}

disa7();


function disa8() {
    const button = document.querySelector('#btn-disabled7');

    const disableButton = () => {
        button.disabled = true;

    };
    button.addEventListener('click', disableButton);
}

disa8();

function disa9() {
    const button = document.querySelector('#btn-disabled8');

    const disableButton = () => {
        button.disabled = true;

    };
    button.addEventListener('click', disableButton);
}

disa9();

